<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Blog App</title>
    <link rel="stylesheet" href="/styles/styles.css" />
  </head>
  <body>
    <%- include('partials/header.ejs') %>
    <div class="container">
      <h1 class="title">Paint Your Thoughts...</h1>
      <div class="form-container">
        <% if (locals.isEdit) { %>
        <p class="form-text">Edit your Blog</p>
        
        <% } %>
  
        <form
          action="<%= locals.isEdit ? `/edit/${locals.blogDetails?.id}` : '/home' %>"
          method="post"
        >
          <label for="bTitle"><h3>Blog Title</h3></label>
          <input
            type="text"
            id="bTitle"
            name="blogTitle"
            placeholder="Your Title Here..."
            required
          />
  
          <label for="bDes"><h3>Blog description</h3></label>
          <textarea
            type="text"
            id="bDes"
            name="blogDes"
            placeholder="Your Blog here..."
            rows="6"
            required
          >
          </textarea>
          <input
            type="submit"
            value="<%= locals.isEdit ? 'EDIT' : 'PUBLISH' %>"
          />
        </form>
      </div>
    </div>
   
    <%- include('partials/footer.ejs') %>
  </body>
</html>